<h2>Create Event</h2>

<div class="event">
	<% form_for(:event, :url => events_path) do |f| %>
		<%= render :partial => 'form' %>
		<%= submit_tag 'Create Event' %>
	<% end %>
</div>

<script>
	Event.observe(window, 'load', function() {
		var center = FNBMap.default_center;
		FNBMap.map.setCenter(center, FNBMap.default_zoom);
	});
</script>
